{
  "name": "iron-trainer",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 8080 --host",
    "build": "vite build",
    "build:dev": "vite build --mode development",
    "check": "vite build",
    "preview": "vite preview --port 8080 --host"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
